{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "incremental": false,    // 禁止缓存，避免 dist 被清空
    "rootDir": "./src",      // 必须指定，否则 tsc 会把 include 的范围外也算进去
    "outDir": "./dist"
  },
  "exclude": ["node_modules", "test", "**/*.spec.ts"]
}